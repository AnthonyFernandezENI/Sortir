{% extends 'base.html.twig' %}

{% block title %}Nouvelle sortie{% endblock %}

{% block body %}
    <div class="grid-container">
        <div class="grid-x grid-margin-x">
            <h3 class="cell small-4 large-offset-4 title-centered">Créer une sortie</h3>
        </div>
        {{ include('sortie/_form.html.twig') }}
        <div class="cell small-12">
            <div class="grid-x grid-margin-x">
                <div class="cell small-4">
                    {{ form_widget(form.creer) }}
                </div>
                <div class="cell small-4">
                    {{ form_widget(form.publier) }}
                </div>
                <div class="cell small-4">
                    <a class="white-font button" href="{{ path('sortie_index') }}">Annuler</a>
                </div>
            </div>
        </div>
    </div>
    {{ form_end(form) }}
    </div>
{% endblock %}
